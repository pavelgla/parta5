# Auth.js secret — generate with: openssl rand -base64 32
AUTH_SECRET=

# PostgreSQL (for local dev)
DATABASE_URL=postgresql://parta5:CHANGE_ME@localhost:5432/parta5

# Docker Compose
POSTGRES_PASSWORD=CHANGE_ME
